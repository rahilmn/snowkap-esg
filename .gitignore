# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# Environment
.env
.env.production
.env.local

# Phase 11A: SQLite WAL sidecar files + backups (never commit these)
data/snowkap.db-wal
data/snowkap.db-shm
data/backups/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Node (frontend)
node_modules/
client/dist/

# Docker volumes (local)
docker-data/
postgres-data/
redis-data/
jena-data/
minio-data/

# Testing
.coverage
htmlcov/
.pytest_cache/

# Logs
*.log
logs/

# Alembic
backend/migrations/versions/__pycache__/

# Compiled
*.so
*.dylib

# Secrets
*.pem
*.key
credentials.json

# Local dev scripts / Claude Code state
.local/
setup_db.py

# Local SQLite DB (transient runtime state — keeps re-conflicting on every rebase)
data/snowkap.db

# Phase 28 cleanup — build artifacts that should never have been
# tracked but slipped through pre-Phase-28.
client/tsconfig.tsbuildinfo
client/.tsbuildinfo
*.tsbuildinfo
~$*.docx
*.paint
stuck.paint

# Phase 28 — Office lockfiles + Replit attachments
attached_assets/
.replit.cache/
